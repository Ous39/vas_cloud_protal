# Secrets — never commit real credentials
.env
.env.*.local
*.pem
*.key
deploy/k8s/*.secret.yaml

# Raw data exports — contain live subscriber/billing PII, must never enter version control
database/source_dump/
database/init/10_hera_production_dump.sql
*_backup.sql

# OS / editor
.DS_Store
Thumbs.db
